# CareBot AI - Requirements
# Install semua: pip install -r requirements.txt

flask==3.0.0
python-dotenv==1.0.0

# ── Provider AI & Fitur Tambahan ──
# Versi diperbarui untuk kompatibilitas dan fitur terbaru
# Menggunakan versi yang ada. Versi >=0.10.0 tidak ada untuk paket 'google-generativeai'.
google-generativeai>=0.5.0
google-cloud-texttospeech>=2.17.0

# Menambahkan protobuf versi spesifik untuk mengatasi TypeError dengan Python 3.12+
protobuf>=4.25.0

# requests adalah dependensi umum, komentar sebelumnya tidak akurat
requests==2.31.0
edge-tts>=6.1.12
pyttsx3>=2.98
